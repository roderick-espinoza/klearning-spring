info:
  name: Contrasena Incorrecta
  type: http
  seq: 3

http:
  method: post
  url: "{{baseUrl}}/api/v1/auth/login"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "username": "{{adminUsername}}",
        "password": "ClaveIncorrecta123"
      }
  auth: inherit

docs: |-
  Debe responder 401 con el cuerpo JSON de ApiErrorResponse.

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
