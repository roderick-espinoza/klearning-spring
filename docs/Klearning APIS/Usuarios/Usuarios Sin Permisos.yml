info:
  name: Usuarios Sin Permisos
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/usuarios"
  headers:
    - name: Authorization
      value: "Bearer {{tokenSinPermisos}}"
  auth: none

docs: |-
  Debe responder 403: el endpoint exige rol ADMIN.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
