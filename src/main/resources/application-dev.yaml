spring:
  datasource:
    url: jdbc:mysql://localhost:3306/klearning?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true
    username: root
    password: root123

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.tool.schema: TRACE
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.SQL: DEBUG