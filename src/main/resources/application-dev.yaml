spring:
  datasource:

    url: jdbc:mysql://localhost:3306/klearning?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:cibertec123}

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

        # >>> AGREGADO: FLUSHING / BATCHING <<<
        jdbc:
          batch_size: 30
        order_inserts: true
        order_updates: true


        generate_statistics: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    org.hibernate.stat: DEBUG
